# Server — set these on your VPS (e.g. in .env or via PM2 ecosystem config)
PORT=3001
MAX_ROOMS=10
CORS_ORIGIN=https://your-domain.com

# Client — set in client/.env.production before building
VITE_SERVER_URL=https://your-domain.com
